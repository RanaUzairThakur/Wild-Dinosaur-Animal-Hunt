using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using ThirdPersonCover;

namespace ThirdPersonCover
{
    public class Animal_Detection : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
       
        // public void OnHit(Hit hit)
        // {

        //     gameObject.GetComponent<Animator>().SetBool("Died", true);
        //    	gameObject.GetComponent<hoMove>().Stop();
            
		// 	// gameObject.GetComponentInParent<hoMove>().Stop();
        //     GameManager_TN90.instance.OnFailAfter(3f);
        //     GameDialogs_TN90.instance.Animalkilledtext.SetActive(true);
        //     //print("Civilians");

        // }
    }
}

